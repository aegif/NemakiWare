<% content_for :javascript_includes do %>
<script type="text/javascript">
	$(function(){
		var options = {
			rules : {
				"group[id]" : {
					required: <%=VALIDATION['group']['id']['required']%>
				},
				"group[name]" : {
					required: <%=VALIDATION['group']['name']['required']%>
				}
			}
		}
  		$("#groupForm").validate(options);
	});
</script>
<% end %>

<!--HEADER-->
<div class="row-fluid span11 nemaki_popup_header">
	<h4><%=t('view.group.new.group_info')%></h4>
</div>

<!--Contents body-->
<div class="nemaki_contents_body">
	<div class="row-fluid">
		<%= form_for :groups , :url => {:action => :create}, :html => {:method => :post, :id => 'userForm', :class => 'form-horizontal'} do |f| %>
			<!--ID-->
			<div class="control-group">
				<label class="control-label" for="inputEmail"><%=t('model.group.id')%></label>
				<div class="controls">
					<%= f.text_field :id %>
				</div>
			</div>
			<!--Display Name-->
			<div class="control-group">
				<label class="control-label" for="inputEmail"><%=t('model.group.name')%></label>
				<div class="controls">
					<%= f.text_field :name %>
				</div>
			</div>
			
			<!--Action-->
			<div class="form-actions nemaki-form-actions">
				<%= submit_tag t('view.group.new.create'), :class => 'btn btn-primary btn-large' %>
			</div>
		<% end %>
	</div>
</div>