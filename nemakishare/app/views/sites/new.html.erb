<h3>新規サイトを作成してください</h3>


<%= form_for @site, :url => {:action => :create} do |f| %>
<div class="field">
	<%= f.label :name %>
	<br/>
	<%= f.text_field :name %>
</div>

<%= submit_tag '新規作成' %>
<% end %>
