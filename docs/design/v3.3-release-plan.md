# v3.3.0 リリース前の対応計画 (2026-08-13)

直近 9 コミット (`ed6a8d9bd^..HEAD`) を対象に、7 次元の批判的レビュー + 各所見の敵対的検証を
並列で回し、併せて Codex に再索引スループット劣化の原因究明を依頼した結果をまとめた計画。

## このレビューの限界 (先に書く)

- 所見 **60 件**を得たが、**検証エージェント 16 本と統合 1 本がセッション上限で落ちた**。
  したがって **24 件のみ敵対的検証済み** (8 件が反証に耐え、16 件棄却)。
- 残り 36 件は**未検証**。そのうち**リリース判断に効く 6 件は筆者が手で裏を取った** (下記 ✔)。
  それ以外は「主張」であって「確定」ではない。**この計画で未検証のものを既成事実として
  扱わないこと。**
- 検証を通っていない所見のうち、blocker / high を名乗るものは**着手前に必ず再検証する**。

---

## 0. リリース可否

**blocker が 2 件。1 件 (B1) は修正済み、1 件 (B2) は未対応。**

- **B1** 全再索引が索引を消したまま「完了」を返しうる → ✅ 再現・修正・回帰テスト済み
- **B2** パッチ再適用でシステムフォルダが二重になる → 🔴 **実地で観測したが未対応**。
  v3.3.0 の初回起動は F4 パッチが design document を書き換えるので、同じ状況を作る
- **B3** 削除済み文書が索引に残りクエリに出る → 🟠 **実地で観測、原因未特定**
  (製品欠陥か、私のコンテナ強制再作成による汚染かを切り分けていない)

製品の CMIS 動作そのものは 4,942 件のスイート + TCK が green で、V1 / V2 / move 委譲などの
今回の変更に機能的な破綻は見つかっていない。止めているのは**アップグレード手順が
索引を破壊しうる**という 1 点。

---

## 1. 出す前に直す

### ✅ B1 (blocker) — **2026-08-13 に再現し、修正した**

**実機で再現した** (`tools/acl-probe/reindex_wipes_index_probe.py`、canopy に 164 文書を入れて):

| | 修正前 | 修正後 |
|---|---|---|
| status | **completed** | **error** |
| totalDocuments / indexedCount | 1 / 1 | 1 / 0 |
| errorCount | **0** | 0 |
| Solr の文書数 | **164 → 1** | **1610 → 1610 (無傷)** |

**障害の注入方法で 2 回外した**。記録しておく:

1. **view を削除する → 効かない。** core を再作成すると起動時に `Patch_StandardCmisViews` が
   走って **view を作り直す** (実測: rev 9 → 10)。この経路はシステムが自己修復する。
2. **map を構文エラーに → CouchDB が PUT を 400 で拒否**。
3. **map を実行時に throw させる → 効く。** そして重要なのは、view クエリが
   **HTTP 200 で `rows:[]`** を返すこと。

3 が設計を決めた。**エラーが起きないので、例外を伝播させる形の修正では閉じない。**
これは推測ではなく実測。したがってガードは**「今 Solr に在る CMIS オブジェクト数」**という、
同じ障害では黙らせられない物差しと突き合わせる形にした
(`-doc_type:[* TO *]` は RAG と CMIS を分ける正典の判別子)。

- 走査が今の索引の 1/10 未満しか見つけなければ **clearIndex に到達させず error で止める**
- Solr に問い合わせられないときは **0 ではなく「不明」として扱い、やはり止める**
  (失敗を空の索引と読むと、まさに塞ぎたいケースを通してしまう)
- 少数 (20 未満) と初回再索引は対象外 — 失うものが無いため

回帰テスト `ReindexRefusesToWipeIndexTest` は**実際の `startFullReindex` を駆動**し、
`clearIndex` の delete が呼ばれないことを検証する。ガードを外すと 2 件落ちる (確認済み)。

<details><summary>元の分析 (機構の説明)</summary>

**機構** (3 つが噛み合う):

1. `ContentDaoServiceImpl.getChildren` は例外を握り潰して**空リストを返す**
   (`core/src/main/java/jp/aegif/nemaki/dao/impl/couch/ContentDaoServiceImpl.java:1143-1146`
   — `return new ArrayList<Content>(); // Return empty list on error`)
2. `startFullReindex` は **件数カウント → `clearIndex` → 走査** の順
   (`SolrIndexMaintenanceServiceImpl.java:138-150`)
3. 走査は folder ごとに catch するが、上記のとおり**例外が来ない**ので `errorCount` も増えない
   (`SolrIndexMaintenanceServiceImpl.java:401-407`)

結果: `children` view が引けない状態で再索引を打つと、**Solr を空にした後に「子が居ない」と
見えて、errors=0 / status=completed のまま空の索引が残る**。しかも完了後の健全性チェックは
同じ走査で母数を作るので **missing=0 / healthy=true** と答える。

**なぜ v3.3.0 で現実的か**: 初回起動で `Patch_JoinedGroupsSingleEmit` (F4) が `_design/_repo` を
書き換え、CouchDB が**全 view を再構築する**。手順書は起動後に全再索引を打てと言う。
再構築中の view クエリはタイムアウトしうる (既知: 起動ログに
`Error getting patch history ... _repo/patch - timeout` が出ることがある、と RELEASE_NOTES に記載済)。

**直し方 (最小・低リスク)**:
- `startFullReindex` の**先頭で view の可用性を確認**し、駄目なら `clearIndex` に到達させず失敗させる。
- 走査側は「子 0 件」と「取得失敗」を区別する。少なくとも**再索引の経路だけは**
  `getChildren` の握り潰しに依存しない (失敗を伝播させる or 明示的に失敗を数える)。
- **完了判定に独立な母数**を使う。`countDocumentsRecursive` と本走査が同じ壊れ方をする限り
  健全性チェックは意味を持たない。CouchDB の `_all_docs` 総数など、走査と独立な数を突き合わせる。

</details>

**残っている関連課題** (B1 の修正では閉じていない):
- `getChildren` / `countDocumentsRecursive` / `collectDocumentIds` の握り潰しは**そのまま**。
  ガードは「索引を壊さない」ことだけを保証する。列挙が静かに欠ける可能性は残る
  (H4 の名前一意性はその別の顔)。
- 完了後の health チェックは今も走査と同じ母数を使うので、**部分的な欠損は検出できない**。

### 🔴 B2 (blocker 候補・**実地で observed**): パッチの再適用でシステムフォルダが二重になる

レビューが**未検証**として挙げていた `PatchUtil.isApplied` の fail-open
(「パッチ履歴 view の照会に失敗したら『未適用』を返す」、`PatchUtil.java:77`) が、
B1 の作業中に**実際に起きた**。推測ではなく観測。

**観測した内容**: bedroom の root に `.system` フォルダが **2 つ**できた。

| id | created | 中身 |
|---|---|---|
| `34169aaa-…` | 2013-01-01 (初期データ) | `users` / `groups` |
| `c61374d7…` | **2026-08-13 12:07:36** | **空** |

`Patch_SystemFolderSetup` が再適用され、空の `.system` を作っている。結果として
**CMIS TCK の `rootFolderTest` が落ちる** — 「id で取ったオブジェクトと path `/.system` で
取ったオブジェクトが一致しない」。パス解決は 1 つしか選べないため。

**引き金**: このとき canopy の design document を書き換えていた。パッチ履歴 view
(`_repo/patch`) は**書き換え対象と同じ design document に同居**しているので、
照会が失敗し「未適用」に倒れたと考えるのが自然。**F4 のパッチ
(`Patch_JoinedGroupsSingleEmit`) は `_design/_repo` を書き換える**ので、
**v3.3.0 の初回起動はまさにこの状況を作る**。

**なぜ blocker 候補か**: v3.3.0 のアップグレードで、
(a) F4 パッチが design doc を書き換える →
(b) 全 view が再構築される →
(c) その間に走る他のパッチの `isApplied` が「未適用」に倒れる →
(d) パッチが再適用され、システムフォルダやコンテンツが二重になる。
本番リポジトリで重複が生まれると、**TCK が落ちるどころか CMIS のパス解決が壊れる**。

**まだ確定していないこと** (着手前に詰める):
- 再適用されたのが `Patch_SystemFolderSetup` だけか、他のパッチもか
  (レビューは「15 本以上が巻き添え」と主張、未検証)
- パッチ履歴が repository 単位か system 単位か。canopy の design doc を壊したのに
  **bedroom に**重複ができた点は、まだ説明できていない
- 冪等でないパッチがどれか

**直し方の方向**: `isApplied` が**照会できないときは「未適用」ではなく失敗**にする
(fail-closed)。パッチを適用しないより、起動を止めて理由を出す方が安い。
併せて、システムフォルダ生成のように**名前で一意であるべきもの**は作る前に存在確認する。

**後始末**: 今回できた空の重複は削除済み (root 子 270 → 269)。

### 🟠 B3 (要調査・**実地で observed**): 削除済み文書が索引に残り、クエリに出る

B1 の作業中に TCK の `Query Smoke Test` が落ちて判明。原因は**孤児の索引エントリ**。

bedroom の Solr に PWC が **3 件** あり、**3 件とも CouchDB では削除済み**だった
(`task_72.txt` / `task_24.txt` / `task_22.txt`)。CMIS のクエリはこれらを返し、
TCK は「1 つの文書が CAN_CHECK_OUT と CAN_CHECK_IN を同時に持つ」として落ちる。

**まだ確定していないこと (原因を私の作業に断定しない)**:
- これらが**製品の欠陥** (PWC の削除が索引に伝わらない) なのか、**私が今日コンテナを
  何度も強制再作成し、再索引を途中で kill した**ことによる汚染なのかを切り分けていない。
- epoch verdict には `COMPLETE_EXCEPT_ORPHANS` という区分が既にあり、**孤児という概念は
  設計が認識している**。ただし「削除済みなのに検索に出る」ことの扱いは別問題。

**なぜリリース前に見るべきか**: 削除した文書が検索結果に出続けるのは、内容によっては
**情報の露出**になる。PWC は作成者しか見えないので今回の 3 件の実害は小さいが、
同じ経路が通常文書で起きるなら話が違う。

**次の一手**: 削除経路 (CMIS delete / deleteTree / PWC の cancelCheckOut) が Solr からの
除去を保証しているかをコードで確認し、保証していないなら**削除の索引反映が落ちたときに
再試行される仕組み** (reconcile キューに載るか) を確かめる。再現は「文書を作る → 削除する →
その間に Solr を止める」で作れるはず。

**後始末**: 今回の 3 件は Solr から除去済み。

### H1 (high ✔検証済み): V2 のインスタンス検査は CMIS 経路にしか無い

`TypeResource.java:530-533` の `@DELETE /repo/{repositoryId}/type/delete/{typeId}` (管理 UI が使う)
は `typeService.deleteTypeDefinition` を**直接**呼び、`TypeManagerImpl.deleteTypeDefinition` の
ガードを丸ごと迂回する (`TypeResource.java:613`)。

RELEASE_NOTES の「インスタンスが残っているタイプは削除できなくなりました」は
**主要経路で偽**。直すか、記述を経路限定にするかの二択で、**直す**方を採る
(検査を `TypeService` 側に下ろすか、REST 経路も TypeManager 経由にする)。

### H2 (high・未検証): CMIS 例外素通しで「本当の失敗」が 404 になる

`TypeManagerImpl.deleteTypeDefinition` は constraint / invalidArgument 以外の**あらゆる例外を
`CmisObjectNotFoundException` に包み直す** (`TypeManagerImpl.java:4089` 付近)。
今回入れた `CmisBaseException` 素通し (`RepositoryServiceImpl.java:494`) はこれもそのまま通すので、
**これまで 500 だった削除の失敗が 404 (=既に無い) としてクライアントに届く**。
クライアントが「消えている」と誤認する。

**着手前に再検証**: 包み直しの範囲と、404 を成功扱いする実クライアントの有無。

### H3 (high・一部検証済み): secondary type はインスタンス検査を通らない

`countByObjectType` の map は `emit(doc.objectType, doc)` で、secondary の適用先は
`Content.secondaryIds` という別フィールド。したがって**使用中の secondary type は今も削除できる**。
V2 が塞いだ穴が secondary では開いたまま。RELEASE_NOTES の書き方もこれに合わせる。

### H4 (medium ✔検証済み): `getChildrenNames` の握り潰しで名前重複が通る

`ContentDaoServiceImpl.java:1416-1418` が catch → 空リスト。消費者は
`ExceptionServiceImpl.java:1384` (作成) と `ContentServiceImpl.java:2871` (リネーム)。
**CouchDB が一瞬落ちると名前一意性チェックが黙って通り、重複名が作られる**。
existContent で直したのと**同じ fail-open**。同じ理屈で塞ぐ。

### D 群 (ドキュメントの事実誤り — 出す前に直す)

| | 何が嘘か | 場所 |
|---|---|---|
| D1 ✔ | 「ログインのたびに全ユーザ文書が転送されていました」— `userItemCache` は **TTL 3600 秒で有効**。転送はキャッシュミス時だけ。効果を最大 3600 倍に誇張している | RELEASE_NOTES.md:28 |
| D2 | F3 について同一ファイル内で自己矛盾。アップグレード手順の直下に「約 5,000 件の leak 警告が未特定の発生源から出る」という**古い記述が残存** | RELEASE_NOTES.md:358 |
| D3 | 「型削除できなくなりました」が REST / 管理 UI で偽 (= H1) | RELEASE_NOTES.md:43 |
| D4 | 「CMIS の拒否が正しい状態コードで返る」が、1 段下の包み直し (= H2) で崩れている | RELEASE_NOTES.md:65 |
| D5 | 「移動時にリクエストを止めなくなりました」— 委譲したのは executor 投入**後**だけで、その手前の `clearCachesRecursively` (上限なしの同期 BFS) は今もリクエストスレッドで走る。**記述された症状は解消していない** | RELEASE_NOTES.md:31 |
| D6 ✔ | 台帳の **ID「R1」が 2 課題で衝突** (既存の RAG 剥奪整合 :174/:895 ✅ と、今回新設の再索引劣化 :696 📋)。:174 の「下記 R1 節」が誤った節に当たる | v3.3-release-blockers.md:696 |
| D7 | §5-1 の運用手順に「本番規模での実測はまだ無い」が残存 (F3 の決着と正面から矛盾) | v3.3-release-blockers.md:1781 |
| D8 | 旧 Javadoc の残存 3 件: `CloudantClientWrapper:1247` (「populated な型の削除は今も通る」)、`TypeManagerImpl:4159` (旧 javadoc と新 javadoc の 2 枚重ね)、`type_delete_constraint_probe.py:9` (「番人は今も発火しない」— 台帳はこのプローブの exit 0 を根拠にしている) | 各所 |
| D9 ✔ | 「呼び出し側 5 箇所は全て `row.getValue()`」が誤り。**6 箇所**で、`UserGroupDaoDelegate:279` は `firstRow.getId()` を使う。**コード変更自体は安全** (getId は include_docs 不要) だが、根拠として書いた不変条件が事実と違う | CloudantClientWrapper.java:1232 |
| D10 | 97,693 の実測は**再索引を完走していない**が、CLAUDE.md / RELEASE_NOTES はその但し書きを落としている | CLAUDE.md:92 |

---

## 2. 出した後でよい (ただし R1 は所要時間として周知が要る)

### R1 系: 全再索引スループット劣化 (Codex 調査、10.3 → 2.6 文書/秒)

Codex の診断は筆者も一次確認した。**主因は「未コミットのバッチを通常クエリで検証している」こと**:

1. **T1 (very high・長年の欠陥、今回の範囲外)**: `flushBatch` はバッチを
   `commitWithin=5000ms` で投げた**直後**に、通常の `solrClient.query()` で検証する
   (`SolrIndexMaintenanceServiceImpl.java:451,546`)。全再索引は先に `clearIndex` するので
   古い可視コピーも無い。→ **ほぼ全件が「missing」と判定され 1 件ずつ再索引される**
   (`:573`)。つまり**全文書が 2 回索引される**。個別再索引は `commitWithin=1000` なので
   ソフトコミットが延々走り、索引が育つほどマージ負荷で遅くなる = **劣化の形になる**。
   - **決定的な確認**: 再索引を 1 回回して `silentDropCount` を見る。総文書数に迫れば確定。
   - 直し方: realtime GET (`/get`) で検証するか、強制コミット後にまとめて検証する。
2. **T2 (high・**今回の C8 で入れた退行**)**: `indexDocumentsBatch` が文書ごとに
   Solr realtime GET + **キャッシュを迂回した CouchDB GET** を足した。
   `ContentIncarnation.resolve` は**既に incarnation を持っていても必ず先に読む**
   (`ContentIncarnation.java:88-93`)。列挙済みの `Content` は既に `content_incarnation` を
   持っているのに取り直している。T1 と重なると 1 文書あたり 2 倍。
   - 直し方: 100 件分の Solr 状態を 1 回で先読みし、incarnation も手元の値を使う
     (fence の意味論は保つ)。
3. **T3 (high・長年)**: 添付が文書あたり **3 回**の CouchDB 往復
   (`extractTextContent` の メタデータ GET + 本体 GET、そして `getContentLength` の
   **もう 1 回のメタデータ GET**)。取得済みの添付オブジェクトが既に length を持っている。
4. **T4 (low)**: 個別 fallback が文書ごとに INFO を数行出す。DEBUG に落とす。

**リリースとの関係**: 正しさには影響しない。ただし v3.3.0 は全再索引が**必須**なので、
**10 万文書で 10 時間級**という所要時間は RELEASE_NOTES に明記する (済)。
T1 だけでも直せば体感で倍近く戻る見込みだが、**索引の可視性に触る変更**なので
リリース後の腰を据えた対応にする。

### O 群: 運用手順 (未検証・要再検証)

- **O1**: 10 時間級の再索引に**再開手段が無い** (毎回 `clearIndex` から、進捗は JVM 内のみ)。
  CLAUDE.md の「時間帯で分割実行してください」は full reindex には成立しない。**記述を直す**。
- **O2**: multi-replica で再索引・初期 epoch stamp を**どのレプリカで打つか**がどの手順書にも無い。
  どちらも JVM ローカル状態。
- **O3**: verdict `COMPLETE` は「索引に在る文書が全部 stamp 済み」であって
  「索引に全文書が在る」ではない。B1 と組み合わさると**空の索引でも COMPLETE**。
- **O4**: ロールバック手順が無い。

---

## 3. 測り直すべきもの

| 何を | どう測るか | なぜコードだけでは決まらないか |
|---|---|---|
| T1 の確定 | 全再索引を 1 回回し `silentDropCount` / `reindexedCount` を読む | 「検証時に可視か」はコミットのタイミング依存 |
| T2 の寄与 | 100 件あたりの Solr realtime GET と CouchDB GET を数える。期待は各 100、二重なら 200 | 実リクエスト数はログ/メトリクスでしか出ない |
| B1 の再現 | `children` view を引けなくして再索引 → completed/errors=0 で空になるか | 例外の型と経路が実際に握り潰されるかは実機依存 |
| CouchDB の劣化 | content GET / 添付メタデータ GET / 本体 GET の**別々の**レイテンシを規模別に | 帰無仮説 (キャッシュ常駐性) を切り分けるため |

---

## 4. 見送る判断

- **F3 の 20 万以上の実測**: オーナー判断で 10 万で打ち切り済み。97,693 でピークが伸びないことは
  確認できており、20 万以上は運用監視で代替する。投入 9 時間 + 再索引 15 時間超は割に合わない。
- **A2 (伝播の Solr バッチ) / B2 (epoch 簿記)**: 当初から 📋 / 計画外。今回の範囲を広げない。
- **`isRoot` の dead parameter** (`AclServiceImpl.java:1169`): 事実だが挙動に影響しない。
  ただし**委譲分岐の真上のコメントが虚偽**なので、コメントだけは直す。
