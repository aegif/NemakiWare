# 3.4 リリース準備 — 残件の整理と、リリース前に要る作業

作成 2026-09-17（HEAD `165ce7013`、ブランチ `fix/v34-fail-closed-reads`）。

役目は 2 つだけ。**(1) 開いている残件を「利用者が踏む限界」「測定の穴」「意図した設計」に
分けること**、**(2) リリースに要る機械的な作業の現在地を書くこと**。

残件の正典は [`fail-closed-reads.md`](fail-closed-reads.md) の表で、ここはその**読み替え**。
表を書き換えるときは向こうが正。巡ごとの経過は
[`../history/fail-closed-review-rounds.md`](../history/fail-closed-review-rounds.md)。

## 0. 現在地

- 表 50 行のうち **処置済み 31 / 開いている 18 / やめた 1**。
- `master` から **169 コミット**、変更 326 ファイル。
- RELEASE_NOTES の「未リリース (3.4 に向けた作業)」節は **1,186 行**（10〜1195 行）。
- 負のコントロール **694 本**。直近の通しは 2026-09-16〜17 に **692/692 発火・不発 0**
  （§5）。その後 R46 で 2 本増えたので、**694 本での通しは走らせていない**。

## 1. 利用者が踏みうる限界（9 件）

「文書」は RELEASE_NOTES に**その限界が書かれているか**。行番号は HEAD 時点。

| ID | 利用者から見える形 | 文書 | リリース判断 |
|---|---|---|---|
| R13 | DLQ 一覧の `offset` に安定ソートが無い。ページを繰る間に行が重複・欠落しうる。DLQ の 1 行は「その項目が失われた」唯一の記録 | **あり** (206–208 行。「安定ソートを持たない」と明記。ただし**ずれる原因を「見ている間の再実行・削除」に限って書いている**) | **要判断** — 直すか、文面を実態（並び順そのものが未規定）に合わせるか |
| R20 | purge は先頭 1,000 行しか走査せず、続きがあることを応答が言わない | **あり** (189 行) | そのまま。製品は不変 |
| R17 | 「読めなかった」を断定に使っていない一覧は、今も空に見えうる | **あり** (952–966 行。正典の残件表を名指し) | そのまま |
| R9 | DLQ の 503 級失敗は `200 "failed"`（403 だけ例外） | **あり** (174–177、220、271 行) | そのまま（意図した限定） |
| R43 | 凍結領域。確定書き込みの fallback 腕が死んでおり、**届いた payload の再実行を拒む**（409） | **なし** | 文面を足すか、残件のまま。製品は凍結 |
| R35 | 凍結領域。token 継承で、確定書き込みの所有権検査の保証が「payload 付きの別保存」に狭い。退行ではない | **なし** | 主張の範囲の話。文面を足すか残置 |
| R44 | 取込後リンクの認可は**フェッチ開始時**のプロファイル行に対して行う（装飾は現在の行で認可するので向きが揃っていない） | **なし** | 残置（読み直しは 1 リンクあたり設定 DB 走査 1 回） |
| R45 | 取込後リンクのパス解決（`targetFolderPath`）で、一過性の読み失敗が拒否になりうる。過剰拒否側の錠は非委譲の腕しか押さえていない | **なし** | 残置 |
| R49 | 取込入口の 503 が例外の `getMessage()` をそのまま本文に入れる。認証済みだが未認可の利用者にも届く。一時ファイルの**絶対パスが入りうる（未実測の推論）** | **なし** | **要判断** — 安く塞げる。R46 の腕と既存 handler の 2 か所 |

R41（接頭辞だけの記録行）は**このブランチの中間ビルドを動かした開発 DB のみ**の話で、
リリース版には接頭辞自体が無い。リリースの限界には数えない。

## 2. 測定の穴（6 件）— 製品欠陥ではない。「測った」と書いてはいけないものの一覧

| ID | 測れていないもの |
|---|---|
| R7 | Mango `_find` が添付 stub を返すという前提 |
| R12 | IMAP IDLE の connect / IDLE ループ / `fetchMessage` の実 I/O |
| R14 | Notion の per-page catch（adapter が `new` される） |
| R15 | コントロール×兄弟錠 2,403 組（掃きはユーザー指示があるまで禁止） |
| R38 | webhook GET 握手の「コネクタ読みが答えなかった → 503」。腕の catch とクラスの `@ExceptionHandler` の二重保護で、1 錨のサボタージュでは測れない |
| R48 | 装飾を `contentService.update` で直接書く扉が増えても、R47 の本数の錠は動かない。`checkOut` / `deleteObject` と、`checkIn` の properties に載せる形もどの錠も数えていない |

## 3. 意図した設計（2 件）

- **R6**: 数値 `repositoryId` はどのリポジトリも名指さない。
- **R16**: `statusOfIdleRefusal` が `raw` も見る（敵対 id は 503 しか買えない非対称）。

## 4. リリースに要る機械的な作業 — 現在地

| 作業 | 現在地 |
|---|---|
| version 3.3.1 → 3.4.0 | **未実施**。`pom.xml` / `core/pom.xml` / `common/pom.xml` / `cloudant-init/pom.xml` の 4 本 |
| RELEASE_NOTES の見出し | **未実施**。「# 未リリース (3.4 に向けた作業)」→「# 3.4.0 (日付)」 |
| 3.4.0 アップグレード運用文書 | **未作成**。先例は [`../operations/v3.3.0-upgrade-runbook.md`](../operations/v3.3.0-upgrade-runbook.md)。3.4 固有は起動時の確定 ID 移行（コネクタ定義・取込プロファイル）。**再索引の要否は未判断** |
| 全ユニットテスト | **HEAD では未実施**。最後の全走は バッチ 4 適用時点の 7,002 本 green。以後バッチ 5〜12 でテストが増えている |
| CMIS TCK 38 本 | **HEAD では未実施** |
| Playwright E2E | **HEAD では未実施** |
| 通し negative-control | 692/692 完走（2026-09-16〜17）。**694 本では未実施**（R46 の BY3 / BZ3 が後） |
| 文書内の版参照の洗い出し | 未実施 |

## 5. この版で主張していないこと

正典 §1 と RELEASE_NOTES の「主張しないこと」8 節がそのまま生きている。特に:

- 取込側の**全読みを直した**とは書かない。直したのは残件表の 31 件。
- **通し negative-control 済み**と書けるのは 692 本の時点まで。
- 上の §2 の 6 件は**測っていない**。
