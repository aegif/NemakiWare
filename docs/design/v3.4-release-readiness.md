# 3.4 リリース準備 — リリースまでにやること / 次に入れるもの

作成 2026-09-17、更新 2026-09-18（HEAD は各行に書いた時点のもの）。

残件の正典は [`fail-closed-reads.md`](fail-closed-reads.md) の表で、**ここはその読み替え**。
表と食い違ったら向こうが正。巡ごとの経過は
[`../history/fail-closed-review-rounds.md`](../history/fail-closed-review-rounds.md)。

急いで出す前提を取っていない。**「リリースまでにやること」は少なく、はっきりした理由が
あるものだけ**にしてある。理由は 3 つしかない:

1. **この版の文書がコードより強い**（＝出した時点で嘘になる）
2. **利用者に見える答えが変わるのに、どこにも書いていない**
3. **リリースの機械的な手続き**（版番号・見出し・運用文書・ゲート）

それ以外は次の版に回す。回す判断の根拠も各行に書く。

## 0. 現在地（2026-09-18）

- 残件表 53 行のうち **処置済み 32 / 開いている 20 / やめた 1**。
- `master` から **197 コミット**。RELEASE_NOTES は `# 3.4.0 (2026-09-17)` の見出しで確定。
- 版番号は **3.4.0**（pom 4 本）。3.4.0 アップグレード運用文書は作成済み。
- 負のコントロール **704 本**。

---

## 1. リリースまでにやること

### 1.1 製品 — すべて処置済み

| ID | 何を | 状態 |
|---|---|---|
| R49 | 取込入口の 503 が例外の `getMessage()` を本文に入れる件（委譲ゲートより手前で返るため未認可の利用者にも届く） | **処置済み。** 理由はログへ、応答は固定文面。錠 2 本、control CF3 / CG3 |
| R51 | ゴミ箱一覧が、アーカイブのある DB では常に 500（**リリースゲートが発見**した既存欠陥。共有 mapper が epoch ミリ秒で書き、SDK が広げた `Double` を読み戻せない） | **処置済み。** `DaoHelper` に deserializer 1 つ。実機で 500 → 100 件。錠 6 本、control CH3 / CI3 / CJ3。確認レビューで 3 件（範囲検査・null 委譲・javadoc 孤児化）を追加処置 |

### 1.2 文書 — すべて記載済み

R43（届いた payload の再実行が 409）/ R35（所有権検査の保証範囲）/ R44（リンクの認可は
フェッチ開始時の行）/ R45（一過性の読み失敗が拒否になりうる）を RELEASE_NOTES に書いた。
製品は触っていない（R43 / R35 は凍結領域、R44 / R45 は 3.4 では据え置き）。

### 1.3 機械的な手続き — 完了

version 3.4.0（pom 4 本。`core/pom.xml` の maven-war-plugin の `3.3.1` は別物なので不変）、
RELEASE_NOTES の見出し、[3.4.0 アップグレード運用手順](../operations/v3.4.0-upgrade-runbook.md)、
文書内の版参照の確認（履歴・計画としての 3.3.1 参照は据え置き。`interpares-mapping.md` の
「現在の master は v3.3.1」だけは master へ取り込む時点で要更新）。

### 1.4 ゲート — 全実施の方針（R51 の処置後にやり直し中）

| ゲート | R51 前の結果 | R51 後 |
|---|---|---|
| 全ユニットテスト | 7,040 本 green | 実行中 |
| デプロイ（3 コンテナ healthy、起動時移行と索引を実機確認） | 済 | 未 |
| CMIS TCK 38 本 | 38/38 green | 未 |
| Playwright E2E | 908 passed / 8 failed / 3 flaky（内訳は §5） | 未 |
| 通し negative-control 704 本 | 未完走（296 本で停止・R51 の処置のため） | 未 |

## 2. 次のリリース（3.4.1）に入れるもの

内訳と判定基準は [`v3.4.1-plan.md`](v3.4.1-plan.md) に分けた。要約:

- **製品 2 件**: R20（purge の応答が「先頭 1,000 行で打ち切った」ことを言う。上限は変えない）、
  R50（印を捨てる公開オーバーロード）。
- **測定 5 件**: R45（過剰拒否側の錠を委譲の腕にも）、R38（複数錨のサボタージュ）、
  R48（3 つ目の数。「捕まえないもの」を先に列挙）、R7（添付 stub の前提を実測）、
  R14（Notion adapter の注入点）。
- **3.5 以降に回すもの**: R43 / R35（凍結を解くかの判断から）、R44（走査 1 回分の
  性能変化が観測可能）、R17（範囲が取込の外へ）、R5（fencing は別件）、
  R12（実セッション）、R15（掃きはユーザー指示待ち）。

## 3. 予定しないもの

| ID | 理由 |
|---|---|
| R6 | 数値 `repositoryId` はどのリポジトリも名指さない — 意図した設計 |
| R9 | DLQ の 503 級失敗は `200 "failed"`（403 だけ例外）— 意図した限定。RELEASE_NOTES 174–177 / 220 / 271 行に記載あり |
| R16 | `statusOfIdleRefusal` が `raw` も見る — 意図した非対称（敵対 id は 503 しか買えない） |
| R41 | 接頭辞だけの記録行は**このブランチの中間ビルドを動かした開発 DB のみ**。リリース版には接頭辞自体が無い |

---

## 4. この版で主張していないこと

正典 §1 と RELEASE_NOTES の「主張しないこと」8 節がそのまま生きている。特に:

- 取込側の**全読みを直した**とは書かない。直したのは残件表の 32 件。
- **通し negative-control 済み**と書けるのは **692 本の時点まで**（704 本では未完走）。
- §2 の測定の穴は**測っていない**。

## 5. Playwright の 8 failed の内訳（R51 前の実行）

- **1 件が本物の製品欠陥** → R51 として処置済み（`Archive Management API › should list archives`）。
- **3 件はデータ不足**: プレビューの 3 本が探す `PDFサンプル.pdf` / `画像サンプル.png` /
  `テキストサンプル.txt` が、確認した範囲（ルート直下 100 件）に無い。
- **4 件は判別できていない**: グループ管理 2・管理者権限 1・ACL 継承 1。同じ spec の隣接
  テスト 3 本が「flaky（再試行で成功）」に出ており、記録にある client-side metadata-XHR
  hang と整合するが、**退行でないと確かめたわけではない**。
