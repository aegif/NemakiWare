# 3.4 リリース準備 — リリースまでにやること / 次に入れるもの

作成 2026-09-17、更新 2026-09-17（HEAD `795737489`、ブランチ `fix/v34-fail-closed-reads`）。

残件の正典は [`fail-closed-reads.md`](fail-closed-reads.md) の表で、**ここはその読み替え**。
表と食い違ったら向こうが正。巡ごとの経過は
[`../history/fail-closed-review-rounds.md`](../history/fail-closed-review-rounds.md)。

急いで出す前提を取っていない。**「リリースまでにやること」は少なく、はっきりした理由が
あるものだけ**にしてある。理由は 3 つしかない:

1. **この版の文書がコードより強い**（＝出した時点で嘘になる）
2. **利用者に見える答えが変わるのに、どこにも書いていない**
3. **リリースの機械的な手続き**（版番号・見出し・運用文書・ゲート）

それ以外は次の版に回す。回す判断の根拠も各行に書く。

## 0. 現在地

- 残件表 51 行のうち **処置済み 30 / 開いている 19 / やめた 1**（R5 は「閉じられない窓」と
  して範囲が確定した行、R32 は文面訂正で閉じた行）。
- `master` から **173 コミット**。RELEASE_NOTES の「未リリース (3.4 に向けた作業)」節は
  **約 1,190 行**。
- 負のコントロール **699 本**。直近の通しは 2026-09-16〜17 に **692/692 発火・不発 0**。
  その後 R46 / R13 で 7 本増えたので、**699 本での通しは走らせていない**。

---

## 1. リリースまでにやること

### 1.1 製品（1 件）

| ID | 何を | なぜリリース前か |
|---|---|---|
| R49 | 取込入口の 503 が例外の `getMessage()` をそのまま本文に入れる件を塞ぐ。R46 の腕と既存の `definitionRowsCouldNotBeRead` handler の 2 か所 | 認証済みだが**未認可**の利用者にも届く。一時ファイルの絶対パスが入りうる（未実測の推論）。理由 2。ユーザー判断で「塞ぐ」 |

### 1.2 文書（利用者が踏むのに、どこにも書いていない限界）

| ID | 書くこと | 判断 |
|---|---|---|
| R43 | 凍結領域。確定書き込みの fallback 腕が死んでおり、**届いた payload の再実行を拒む**（409） | **書く。** 症状の類（409 → コネクタから取り直し）は 203–205 行にあるが、この引き金は書かれていない |
| R35 | 凍結領域。token 継承で、確定書き込みの所有権検査の保証が「payload 付きの別保存」に狭い | **書く。** 退行ではないが、**保証の範囲**の話なので黙ると 1 の「文書がコードより強い」に当たる |
| R44 | 取込後リンクの認可は**フェッチ開始時**のプロファイル行に対して行う（装飾は現在の行で認可するので向きが揃っていない） | **書く（1 文）。** 認可がいつの行に対してかは利用者が読む事実 |
| R45 | 取込後リンクのパス解決で、一過性の読み失敗が拒否になりうる | **書く（1 文）。** 過剰拒否は利用者に見える |

### 1.3 機械的な手続き

| 作業 | 現在地 |
|---|---|
| version 3.3.1 → 3.4.0 | 未実施。`pom.xml` / `core/pom.xml` / `common/pom.xml` / `cloudant-init/pom.xml` の 4 本 |
| RELEASE_NOTES の見出し | 未実施。「# 未リリース (3.4 に向けた作業)」→「# 3.4.0 (日付)」 |
| 3.4.0 アップグレード運用文書 | 未作成。先例は [`../operations/v3.3.0-upgrade-runbook.md`](../operations/v3.3.0-upgrade-runbook.md)。3.4 固有は**起動時の確定 ID 移行**（コネクタ定義・取込プロファイル）と、**この起動で登録される Mango 索引 `idx_type_dlqId_id`**。**再索引の要否は未判断** |
| 文書内の版参照の洗い出し | 未実施 |

### 1.4 ゲート

| ゲート | 現在地 | 判断 |
|---|---|---|
| 全ユニットテスト | HEAD では未実施。最後の全走は バッチ 4 時点の 7,002 本 green。以後 バッチ 5〜13 でテストが増えている | **走らせる**（合意済み）。1 時間前後 |
| 通し negative-control 699 本 | 692 本で完走。7 本が未参加 | **走らせるべき。** 正典 §5 の「通し済み」は 692 本の時点までしか言えない。約 17 時間。**指示待ち** |
| CMIS TCK 38 本 | HEAD では未実施 | **走らせるべき。** 取込側の変更だが、`IngestJobService` は共有の設定 DB を触る。要デプロイ。**指示待ち** |
| Playwright E2E | HEAD では未実施 | 同上。**指示待ち** |

---

## 2. 次のリリース（3.4.1）に入れるもの

内訳と判定基準は [`v3.4.1-plan.md`](v3.4.1-plan.md) に分けた。要約:

- **製品 2 件**: R20（purge の応答が「先頭 1,000 行で打ち切った」ことを言う。上限は変えない）、
  R50（印を捨てる公開オーバーロード）。
- **測定 5 件**: R45（過剰拒否側の錠を委譲の腕にも）、R38（複数錨のサボタージュ）、
  R48（3 つ目の数。「捕まえないもの」を先に列挙）、R7（添付 stub の前提を実測）、
  R14（Notion adapter の注入点）。
- **3.5 以降に回すもの**: R43 / R35（凍結を解くかの判断から）、R44（走査 1 回分の
  性能変化が観測可能）、R17（範囲が取込の外へ）、R5（fencing は別件）、
  R12（実セッション）、R15（掃きはユーザー指示待ち）。

## 3. 予定しないもの

| ID | 理由 |
|---|---|
| R6 | 数値 `repositoryId` はどのリポジトリも名指さない — 意図した設計 |
| R9 | DLQ の 503 級失敗は `200 "failed"`（403 だけ例外）— 意図した限定。RELEASE_NOTES 174–177 / 220 / 271 行に記載あり |
| R16 | `statusOfIdleRefusal` が `raw` も見る — 意図した非対称（敵対 id は 503 しか買えない） |
| R41 | 接頭辞だけの記録行は**このブランチの中間ビルドを動かした開発 DB のみ**。リリース版には接頭辞自体が無い |

---

## 4. この版で主張していないこと

正典 §1 と RELEASE_NOTES の「主張しないこと」8 節がそのまま生きている。特に:

- 取込側の**全読みを直した**とは書かない。直したのは残件表の 30 件。
- **通し negative-control 済み**と書けるのは **692 本の時点まで**（699 本では未実施）。
- §2.2 の 7 件は**測っていない**。
