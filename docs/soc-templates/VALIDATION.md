# SOC template validation — last automated run

Generated by `scripts/validate-soc-templates.sh` —
do NOT edit this file by hand; re-run the script to update.

**Run timestamp**: 2026-05-23 02:38:46 UTC
**Branch**: release/3.1.1-RC6
**Commit**: 77ae494b6
**Docker phase ran**: yes

## Summary

- PASS: 20
- SKIP: 3
- FAIL: 0

## Detail

### Phase 1.1 — JSON parse (NDJSON)

| Status | Check |
|---|---|
| ✅ PASS | kibana-detection-rules.ndjson — 5/5 lines valid JSON |

### Phase 1.2 — YAML parse

| Status | Check |
|---|---|
| ✅ PASS | filebeat-nemakiware.yml — YAML parses |
| ✅ PASS | loki-ruler-rules.yml — YAML parses |

### Phase 1.3 — TOML parse

| Status | Check |
|---|---|
| ⚠️ SKIP | vector-nemakiware.toml — python tomllib unavailable (needs Python 3.11+) |

### Phase 1.4 — NUL-byte smoke (RC6.1 P2-3 regression class)

| Status | Check |
|---|---|
| ✅ PASS | filebeat-nemakiware.yml — 0 NUL bytes |
| ✅ PASS | loki-ruler-rules.yml — 0 NUL bytes |
| ✅ PASS | fluent-bit-nemakiware.conf — 0 NUL bytes |
| ✅ PASS | splunk-savedsearches.conf — 0 NUL bytes |
| ✅ PASS | vector-nemakiware.toml — 0 NUL bytes |
| ✅ PASS | kibana-detection-rules.ndjson — 0 NUL bytes |

### Phase 1.5 — file-type smoke

| Status | Check |
|---|---|
| ✅ PASS | filebeat-nemakiware.yml — Unicode text, UTF-8 text |
| ✅ PASS | loki-ruler-rules.yml — Unicode text, UTF-8 text |
| ✅ PASS | fluent-bit-nemakiware.conf — Unicode text, UTF-8 text |
| ✅ PASS | splunk-savedsearches.conf — Unicode text, UTF-8 text |
| ✅ PASS | vector-nemakiware.toml — Unicode text, UTF-8 text |
| ✅ PASS | kibana-detection-rules.ndjson — JSON data |

### Phase 1.6 — placeholder enumeration

| Status | Check |
|---|---|
| ✅ PASS | 16 distinct placeholders enumerated (informational — operator fills before deploy) |
| ℹ️ INFO | placeholder: `${BURST_THRESHOLD:-20}` |
| ℹ️ INFO | placeholder: `${BUSINESS_HOURS_END_LOCAL:-22}` |
| ℹ️ INFO | placeholder: `${BUSINESS_HOURS_START_LOCAL:-06}` |
| ℹ️ INFO | placeholder: `${BUSINESS_HOURS_TZ:-UTC}` |
| ℹ️ INFO | placeholder: `${BUSINESS_HOURS_TZ}` |
| ℹ️ INFO | placeholder: `${HOSTNAME}` |
| ℹ️ INFO | placeholder: `${LOST_COUNT_OUTLIER_THRESHOLD:-50}` |
| ℹ️ INFO | placeholder: `${NEMAKIWARE_AUDIT_LOG_PATH}` |
| ℹ️ INFO | placeholder: `${PLACEHOLDER}` |
| ℹ️ INFO | placeholder: `${SIEM_API_KEY}` |
| ℹ️ INFO | placeholder: `${SIEM_ENDPOINT}` |
| ℹ️ INFO | placeholder: `${SIEM_HOST}` |
| ℹ️ INFO | placeholder: `${SIEM_PASSWORD}` |
| ℹ️ INFO | placeholder: `${SIEM_PORT}` |
| ℹ️ INFO | placeholder: `${SIEM_SPLUNK_INDEX}` |
| ℹ️ INFO | placeholder: `${SIEM_USERNAME}` |

### Phase 2.1 — vector validate (timberio/vector)

| Status | Check |
|---|---|
| ✅ PASS | vector-nemakiware.toml — vector validate passed (--skip-healthchecks) |

### Phase 2.2 — fluent-bit --dry-run (fluent/fluent-bit)

| Status | Check |
|---|---|
| ✅ PASS | fluent-bit-nemakiware.conf — fluent-bit --dry-run passed |

### Phase 2.3 — filebeat test config (docker.elastic.co/beats/filebeat)

| Status | Check |
|---|---|
| ✅ PASS | filebeat-nemakiware.yml — filebeat test config passed |

### Phase 2.4 — cortextool rules check (grafana/cortex-tools, backend=loki)

| Status | Check |
|---|---|
| ✅ PASS | loki-ruler-rules.yml — cortextool rules check passed (backend=loki, envsubst'd) |

### Phase 2.5 — Manual gates (not run; operator-side)

| Status | Check |
|---|---|
| ⚠️ SKIP | kibana-detection-rules.ndjson — Elastic 8 cluster import (Security → Manage rules → Import) — operator gate |
| ⚠️ SKIP | splunk-savedsearches.conf — splunk btool savedsearches list — operator gate |

